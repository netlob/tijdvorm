## Home Assistant (optional)
## Used by main.py to filter explicit eastereggs when HA boolean is off.
##
## Copy this to `.env` (same folder as start.sh) and fill values.
## NOTE: Don't commit your `.env` (it contains secrets).

HA_BASE_URL=
HA_TOKEN=
HA_EXPLICIT_ENTITY=input_boolean.explicit_frame_art
HA_TIMEOUT_SECONDS=2.0
HA_CACHE_TTL_SECONDS=30.0
HA_SAUNA_ENTITY=
HA_DRYER_ENTITY=
HA_TEMP_ENTITY=


